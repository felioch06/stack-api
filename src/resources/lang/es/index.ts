import onboarding from './onboarding';
import version from './version';

export default {
  version,
  onboarding,
  serverError:
    'Ha ocurrido un error al realizar la operación. Intente de nuevo más tarde',
};
